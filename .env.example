# Copy to .env and set your PostgreSQL URL.
# If connection is refused, PostgreSQL may be on port 5433 (common on Windows).
# DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/aistocks
# DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5433/aistocks

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/aistocks
